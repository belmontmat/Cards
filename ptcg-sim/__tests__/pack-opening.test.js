import {breakPack, getRandomIntInclusive, fetchCards} from '../index.js';

describe('breakPack', () => {
  it('there are no dupes in a pack (minus the reverse)', () => {
    //expect(2+2).toBe(4);
  });
  it('should always give 3 commons, 2 uncommons, a reverse(holo-), and a rare+ card', () => {

  });
  it('should be pulling from the correct set', () => {

  });
});
