// These tests should be testing how the components render
import {breakPack, getRandomIntInclusive, fetchCards, addCards} from '../index.js';

describe('render', () => {
  //input mock packs
    it('should render 10 cards', () => {

    });
    it('each component has correct image', () => {

    });
    it('each component has a correct price', () => {

    });
});
describe('button', () => {
    it('should send correct data to breakPack', () => {

    });
    it('should update money correctly after render', () => {

    });
});
