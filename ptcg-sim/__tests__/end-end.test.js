//These should test that the app renders the correct data from hitting the button
import {breakPack, getRandomIntInclusive, fetchCards, addCards} from '../index.js';
import import App from './App';

describe('end-to-end', () => {
  it('should render all 10 new cards on button press', () => {

  });
});
